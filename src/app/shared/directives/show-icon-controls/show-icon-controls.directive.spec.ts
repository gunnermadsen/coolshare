import { ShowIconControlsDirective } from './show-icon-controls.directive';

describe('ShowIconControlsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIconControlsDirective();
    expect(directive).toBeTruthy();
  });
});
