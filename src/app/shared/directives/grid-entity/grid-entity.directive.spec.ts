import { GridEntityDirective } from "./grid-entity.directive";

describe('GridEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new GridEntityDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
